import { MainLayoutDirective } from './main-layout.directive';

describe('MainLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new MainLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
